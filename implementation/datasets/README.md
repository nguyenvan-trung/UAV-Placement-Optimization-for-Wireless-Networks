# Datasets

- `raw/`: giữ nguyên dữ liệu ban đầu, không sửa trực tiếp.
- `processed/`: dữ liệu đã kiểm tra và chuẩn hóa để thuật toán sử dụng.

Schema đề xuất cho người dùng mặt đất: `user_id,x,y,demand`.
