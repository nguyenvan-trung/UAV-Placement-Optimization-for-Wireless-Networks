# Results

Khi chạy thí nghiệm, chương trình sẽ tạo:

- `tables/`: CSV tổng hợp coverage, objective, convergence và runtime.
- `figures/`: biểu đồ so sánh.
- `convergence/`: lịch sử giá trị mục tiêu theo vòng lặp.
